model:
  hidden_dim: 64

ppo:
  learning_rate: 0.001
  gamma: 0.99
  clip_ratio: 0.2
  value_coefficient: 0.5
  entropy_coefficient: 0.05
  update_epochs: 4
  minibatch_size: 128
  max_grad_norm: 0.5

training:
  updates: 40
  episodes_per_update: 32

runtime:
  device: auto
  seed: 42
